# Contributing

| Command      | Description                              |
|--------------|------------------------------------------|
| `make format` | Format source code with `ruff`          |
| `make lint`   | Lint and auto-fix source code with `ruff` |
| `make test`   | Run the test suite with `pytest`        |
| `make all`    | Run `format`, `lint`, and `test` in sequence |